// Condicionales

const disponible = 6000
const retirar = 7500

if( disponible > retirar ) {
    console.log('Si puedes retirar') // Si se cumple la condición se ejecuta el código
} else {
    console.log('Lo siento, no puedes retirar') // Si no se cumple la condición, ejecutar este código
}


// > - Mayor Que
// < - Menor que
// >=  Mayor o igual
// <= Menor o igual

